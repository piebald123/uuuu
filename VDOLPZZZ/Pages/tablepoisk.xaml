<Page x:Class="VDOLPZZZ.Pages.tablepoisk"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:VDOLPZZZ.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="tablepoisk">

    <Grid>
        <StackPanel Orientation="Vertical" Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" Background="Bisque">
            <TextBlock HorizontalAlignment="Center" FontSize="15" Text="Выберите таблицу" FontWeight="Bold" Margin="0, 0, 0, 10"></TextBlock>
            <Button x:Name="AddBtn" Content="Учетная" Margin="5" Height="30" Width="150" Click="AddBtn_Click"></Button>
            <Button x:Name="DelBtn" Content="Справочная" Margin="5" Height="30" Width="150" Click="DelBtn_Click"></Button>
            <Button x:Name="RefBtn" Content="Назад" Margin="5" Height="30" Width="150" Click="RefBtn_Click"></Button>
        </StackPanel>

    </Grid>
</Page>
